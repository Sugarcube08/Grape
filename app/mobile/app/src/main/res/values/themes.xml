<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Theme.Grape" parent="@android:style/Theme.Material.NoActionBar" />

    <!-- Splash Screen theme -->
    <style name="Theme.Grape.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/grape_splash</item>
        <item name="windowSplashScreenBackground">#090013</item>
        <item name="postSplashScreenTheme">@style/Theme.Grape</item>
    </style>
</resources>
